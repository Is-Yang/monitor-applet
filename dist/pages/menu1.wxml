
    <view class="menu1-container" style="padding-top: {{statusBarHeight + 44}}px">
        <van-nav-bar title="监测预警" fixed="fixed" custom-class="nav-reset">
            <view  wx:if="{{isBindDept}}" slot="left" bindtap="toReport">
                上报预警
            </view>
        </van-nav-bar>
        <view  wx:if="{{isBindDept}}" class="screen-page-wrap" style="margin-top: {{statusBarHeight}}px">
            <view class="box-view">
                <view class="left-screen">
                    <view class="opt" bindtap="showScreenTime">筛选
                        <van-icon name="arrow-down"/>
                    </view>
                    <view class="opt">分类
                        <van-icon name="arrow-down"/>
                    </view>
                </view>
                <view class="right-page">
                    <text>本月：{{page.count}} 条</text>
                    <text class="split"> | </text>
                    <text>总共{{page.total}}条</text>
                </view>

                <view wx:if="{{timeScreen}}" class="screen-time">
                    <view class="opt" wx:for="{{timeOptions}}" wx:key="index" bindtap="selectTime">{{item.label}}</view>
                </view>
            </view>
        </view>
        <view class="list-wrap">
            <view wx:for="{{listData}}" wx:key="index" wx:index="index" wx:for-item="item" class="item {{item.state == 0 ? 'not' : ''}}" data-id="{{item.yuJingId}}" bindtap="toDetails">
                <view class="content">
                    <image src="{{item.picture}}" class="picture"/>
                    <view class="full">
                        <view class="title">{{item.device.deviceName}}</view>
                        <view class="desc">{{item.description}}</view>
                        <text class="time"> {{item.createTime}}</text>
                    </view>
                </view>
                <view class="user-info">
                    <view class="name">{{item.createBy}}</view>
                    <text class="state">{{item.state == 0 ? '未处理' : '已处理'}}</text>
                </view>
            </view>
             <view wx:if="{{listData.length == 0}}" class="no-data">
                <text>无预警信息</text>
            </view>
        </view>
    </view>