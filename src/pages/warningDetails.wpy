<template>
    <view class="warning-details-container">
        <van-nav-bar title="预警详情" left-arrow left-text="返回" fixed custom-class="nav-reset" bind:click-left="onClickLeft" />
    </view>
</template>

<script>
import wepy from 'wepy'
export default class WarningDetails extends wepy.page {
    config = {
        navigationBarTitleText: '预警详情',
        usingComponents: {
            "van-nav-bar": "../components/vant/nav-bar/index"
        }
    }

    data = {
        statusBarHeight: wepy.$instance.globalData.statusBarHeight,
    }

    methods = {
        onClickLeft() {
            wx.navigateBack({
                delta: 1
            });
        }
    }
}
</script>

